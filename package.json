{
  "name": "@ked57/agent-config",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "check": "node --check bin/agent-config.mjs && node --check bin/native-agents.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  }
}
