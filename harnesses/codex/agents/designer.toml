# agent-config:managed
name = "designer"
description = "Direct AI to produce product design work and return a design report with evidence."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
developer_instructions = "Read `~/.agents/agents/designer.md` and follow it. If unavailable, report the missing file and stop."
