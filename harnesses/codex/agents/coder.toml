# agent-config:managed
name = "coder"
description = "Implement a scoped plan and return verification evidence."
model = "gpt-5.6-sol"
model_reasoning_effort = "low"
developer_instructions = "Read `~/.agents/agents/coder.md` and follow it. If unavailable, report the missing file and stop."
